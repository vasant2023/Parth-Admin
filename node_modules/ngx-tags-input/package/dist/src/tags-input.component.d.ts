import { OnInit, EventEmitter } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
export declare class TagsInputComponent implements OnInit, ControlValueAccessor {
    private selected;
    tags: any[];
    private onTouchedCallback;
    private onChangeCallback;
    maxTags: number;
    removeLastOnBackspace: boolean;
    canDeleteTags: boolean;
    placeholder: string;
    options: any;
    displayField: string;
    onTagsChanged: EventEmitter<{}>;
    onMaxTagsReached: EventEmitter<{}>;
    onNoOptionsMatch: EventEmitter<{}>;
    ngOnInit(): void;
    private getPlaceHolder();
    private tagsChanged(type, tag);
    private removeLastTag(tagInput);
    private addTag(tagInput);
    private addPredefinedTag(tag);
    private removeTag(tagToRemove);
    private maximumOfTagsReached();
    private isDeleteable(tag);
    private typeaheadOnSelect(e);
    private typeaheadOnNoMatch(e);
    writeValue(value: any): void;
    registerOnChange(fn: any): void;
    registerOnTouched(fn: any): void;
}
